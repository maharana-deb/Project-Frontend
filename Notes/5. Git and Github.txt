version control

Git - is a program for managing source code
Github - hosts a copy of that code and repository online

git download for windows

git -v 

open terminal

1. git init
2. git add . 

U - untracked
A - added

3. git commit -m "message"
    git config --global user.email "email_id"
    git config --global user.name "username"

How to push to github

git remote add origin "link"
git push -u origin master

git remote remove origin

setting -> pages -> branch select -> save

*main code should contain index.html

https://maharana7.github.io/html/


clone in github

open folder -> cmd -> code . -> git clone "link"
